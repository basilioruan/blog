import styled, { css } from 'styled-components';

export const MainContent = styled.div`
  ${(theme) => css`
    padding: 20px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  `}
`