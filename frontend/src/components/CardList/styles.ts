import styled, { css } from 'styled-components';

export const MainContent = styled.div`
  ${(theme) => css`
    padding: 64px;
    margin-left: 120px;
    margin-right: 120px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  `}
`